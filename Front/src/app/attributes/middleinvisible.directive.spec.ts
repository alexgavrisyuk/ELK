import { MiddleinvisibleDirective } from './middleinvisible.directive';

describe('MiddleinvisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddleinvisibleDirective();
    expect(directive).toBeTruthy();
  });
});
